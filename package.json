{
  "name": "mail-gateway",
  "version": "1.0.0",
  "description": "A very simple Express server for sending email messages using Nodemailer. It has a basic REST API with a single route.",
  "main": "index.js",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cookie-parser": "^1.4.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "nodemailer": "^6.4.6"
  },
  "devDependencies": {
    "nodemon": "^2.0.3"
  },
  "author": "",
  "license": "MIT",
  "scripts": {
    "start": "./node_modules/.bin/nodemon ./index",
    "debug": "./node_modules/.bin/nodemon --inspect ./index",
    "test": "mocha --timeout 2000 -b test/test"
  }
}
